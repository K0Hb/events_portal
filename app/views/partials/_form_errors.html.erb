<% errors = model.errors %>

<% if errors.any? %>
  <div class="alert alert-danger alert-danger">
    <div class="lead buffer-bottom">
      В вашей форме <%= t("errors.messages.not_saved", count: errors.count) %>
    </div>

    <ul class="alert alert-danger alert-danger">
      <% errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
