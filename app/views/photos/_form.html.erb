<%= form_for [@event, photo], html: { multipart: true } do |f| %>
  <%= render partial: 'partials/form_errors', locals: {model: photo} %>

  <div class="form-group">
    <%= f.file_field :photo %>
  </div>

  <div class="actions">
    <%= f.submit t('view.download_photo'), class: 'btn btn-primary mt-2' %>
  </div>
<% end %>
